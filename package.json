{
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.2.3",
    "joi": "^18.0.2",
    "multer": "^2.0.2",
    "mysql2": "^3.15.3"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "supertest": "^7.1.4"
  },
  "scripts": {
    "sync-env": "node scripts/sync-env.js",
    "start": "npm run sync-env && cd client && npm start",
    "dev": "npm run sync-env && concurrently \"cd client && npm start\" \"cd backend && npm run dev\""
  }

}
